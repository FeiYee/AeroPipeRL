# AeroPipeRL 系统优化改造方案
## 现有架构：核心分层架构已验证，针对现有问题改造方案如下，按优先级排序：

---

## 🔝 优先级最高：核心性能提升（2周可完成，改动量<500行）
### 问题1：高层规划无全局多智能体协同改造
**现状：当前Planner为每个智能体独立规划，无全局冲突消解
**改造方案：
1. 扩展Planner输入：将existing_route_onehot参数扩展为all_agent_planned_routes（所有智能体规划路径分布）
2. 增加全局流量约束：在edge_flow_scores计算时添加边占用惩罚，已被占用的边权重降低
3. 增加路径冲突损失：Planner损失中加入全局流量正则项，惩罚多智能体路径重叠
4. 调整规划奖励：将全局冲突率加入Planner奖励函数，倒逼主动分流
**修改文件：`algorithms/path_planning.py`、`training/trainer.py`
**预期收益：多智能体全局冲突率下降60%+，整体通行效率提升30%+

---

### 问题3：高低层"两张皮"问题改造
**现状：高低层目标不对齐，低层可能偏离高层规划路线
**改造方案：
1. 扩展高层子目标输出：新增子目标截止时间、优先级、允许偏离范围三个参数
2. 低层增加约束：奖励函数增加子目标按时完成奖励，超时/超范围给惩罚并触发重规划
3. 增加高层主动干预：高层每50步全局检查智能体进度，主动重规划进度落后/走偏的智能体
**修改文件：`algorithms/path_planning.py`、`algorithms/obstacle_avoidance.py`、`training/trainer.py`
**预期收益：子目标达成率从70%提升至95%+，彻底解决低层绕路问题

---

## ⚔️ 优先级中等：鲁棒性提升（1周可完成，改动量<150行）
### 问题2：对抗训练半残废改造
**现状：AdversaryActor定义但未实际参与训练，无鲁棒性增益
**改造方案：
1. 接入环境逻辑：管道节点通行容量乘以Adversary输出的capacity_compress，模拟动态拥堵
2. 补全对抗训练流程：每训练3次主策略，训练1次Adversary，Adversary损失为 -成功率 + 碰撞率，最大化主策略难度
3. 自适应对抗强度：训练前期低强度，后期逐步提升，避免前期训练崩溃
**修改文件：`environment.py`、`training/trainer.py`
**预期收益：拥堵/障碍场景下成功率从40%提升至80%+，极端场景鲁棒性大幅提升

---

## 🚀 优先级稍低：大规模场景支持（3周可完成，改动量<500行）
### 问题4：多智能体协同真实性不足改造
**现状：全连接全局通信不符合真实物理约束，无全局调度机制
**改造方案：
1. 通信加距离掩码：低层通信仅允许距离<10米的智能体交互，符合真实通信范围约束
2. 高层加全局调度：单行道管道增加流量调度、时间槽分配，避免相向冲突
3. 增加局部协商机制：智能体通过通信协商窄管道通行优先级，避免硬避碰
4. 去中心化评论家改造：替换现有中心化评论家改为注意力机制去中心化，支持任意数量智能体扩展
**修改文件：`algorithms/obstacle_avoidance.py`、`models/critic.py`、`environment.py`
**预期收益：支持32个以上智能体同时作业，通行效率再提升40%，符合真实部署物理约束

---

## 📅 整体改造路线
```
第一阶段（2周）：完成问题1+3改造 → 核心性能达标，16智能体成功率>90%
第二阶段（1周）：完成问题2改造 → 鲁棒性达标，拥堵场景成功率>80%
第三阶段（3周）：完成问题4改造 → 大规模场景支持，32智能体流畅作业
```
所有改造均在现有框架扩展，无需重构核心逻辑，可边改边测。
